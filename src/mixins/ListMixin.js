// import bus from '../utils/bus'

// mixin
export default {
  // 재사용할 컴포넌트 옵션
  created() {
    // bus.$emit('start:spinner');
    // // #1 데이터호출
    // this.$store.dispatch('FETCH_LIST', this.$route.name)
    //   .then(() => {
    //     //#5
    //     console.log(5) // 순서를 알아보기 위해 4,5번 콘솔을 찍어보자.
    //     console.log('fetched');
    //     bus.$emit('end:spinner');
    //   })
    //   .catch((error) => {
    //     console.log(error);
    //   });
  }
}